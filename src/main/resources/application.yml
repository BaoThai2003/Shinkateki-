spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shinkateki
    username: root
    password: Thaibao3005@

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
